spring:
  profiles:
    active: @profile.active@
    group:
      dev: nacos,customs
      prod: nacos,customs
  datasource:
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root

server:
  port: 9006

mybatis-plus:
  type-aliases-package: com.test.entity,com.test.dto,
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
