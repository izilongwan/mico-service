spring:
  application:
    name: eureka-feignapi

  cloud:
    nacos:
      server-addr: localhost:8848

feign:
  httpclient:
    enabled: true
    max-connections: 200
    max-connections-per-route: 50
